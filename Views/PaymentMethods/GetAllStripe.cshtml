@model IEnumerable<MVCBookingFinal_YARAB_.Models.StripePaymentMethod>

@{
    ViewData["Title"] = "GetAllStripe";
}

<h1>GetAllStripe</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.AccountNumber)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PaymentType)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.AccountNumber)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PaymentType)
            </td>
            <td>
                    <a asp-action="EditStripeMethod" asp-route-id="@item.Id">Edit</a> |
                    <a asp-action="GetStripeMethodById" asp-route-id="@item.Id">Details</a> |
                    <a asp-action="DeleteStripeMethod" asp-route-id="@item.Id">Delete</a>
                   

                   
            </td>
        </tr>
}
    </tbody>
</table>
<a asp-action="Index">General Payments</a>
